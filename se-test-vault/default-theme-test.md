---
theme: black
---

# Default Theme Test

`theme: black` (no extension) in frontmatter. The settings-tab theme
picker always inserts a full filename with `.css`, so this bare-name
form can only be exercised via frontmatter, not the UI.

Should resolve via the bundled `dist/theme/black.css` fallback.

---

## Confirm the theme applied

If this deck renders with the black reveal.js theme, the bare `theme:
black` value resolved correctly.
